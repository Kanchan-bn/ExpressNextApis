import React, { useState } from 'react';
import data from '../api/util/11YVCHAR001.json';

export const WineDetails = ({input}) => {
    console.log(data);
    // const fileName = input;
    // const inputData = fetch(`http://localhost:3000/getYearBreakdown/${fileName}`)
    // .then(response => response)
    // .catch(err => console.log(err));

    // const [data, setData] = useState({});
    // console.log(inputData);

    return (
        <>
        wine details
        </>
    )
}